# Open Records Manager

## Contribution

## Building

To build sources locally, use the following instructions.

### Requirements

- Java 25 - Required to build

### Build and Run

Run the `run-server` Maven profile to run the server
Run the `package` task to build the server
